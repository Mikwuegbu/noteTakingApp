{
	"name": "notetakingapp",
	"version": "1.0.0",
	"description": "This is a simple note taking app to demonstrate the use of tailwind &amp; Javascript in Dom manipulation and storing in the localStorage",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"dev": "npx tailwindcss -i ./src/input.css -o ./dist/output.css --watch"
	},
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"tailwindcss": "^3.4.1"
	}
}
